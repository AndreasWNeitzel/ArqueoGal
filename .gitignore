# Context
context/

# Personal working-instructions (not part of the published project)
AGENTS.md

# Logs
logs/

# Images: never committed (figures and gallery PNGs stay local).
*.png
*.PNG
*.jpg
*.JPG
*.jpeg
*.JPEG
*.gif
*.GIF
*.svg
*.SVG
*.webp
*.bmp
*.tif
*.tiff
*.ico
*.pdf
*.PDF

# Filter-repo backup snapshot
.git-backup-before-filter/

# Reports tree: stays local, not part of the GitHub publication surface.
# Includes gallery PNGs, training-run logs, calibration JSONs, audit chain
# reports, sanity-battery figures, and figure outputs.
reports/
# ...but whitelist the dated technical surveys referenced from docs/plan/.
!reports/codebase_survey_2026-04-19.md
!reports/storage_cleanup_2026-04-19.md

# Methods-paper drafts: stays local until journal submission.
paper/

# Multi-agent review-of-reviews drafts: internal-only.
review_of_reviews/

# Internal review workspaces and ad-hoc agent output.
.expert_review_*/

# WSL2 / system tuning configs: local infrastructure, not for publication.
etc/

# Data — never committed
data/raw/
data/interim/
data/processed/
data/external/
data/fire2/
# ...but keep README stubs and directory markers
!data/**/README.md
!data/**/.gitkeep
# ...and keep provenance sidecars (they ARE committed — see data_acquisition.md §14.4)
!data/provenance/
!data/provenance/**

# Trained models
models/
!models/**/README.md
!models/**/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Jupyter
.ipynb_checkpoints/

# Environment
.env
*.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Ruff / mypy / pytest cache
.ruff_cache/
.mypy_cache/
.pytest_cache/

# aim experiment tracking DB (Phase 5)
.aim/

# Local D-Cat-b release-format candidates (large parquets; regenerated from
# tracked code, not part of the GitHub release surface).
release/D-Cat-b/
release/test_ablations_*/predictions_stream*.parquet
release/test_ablations_*/predictions_stream*.parquet.provenance.json
# release/test_ablations_*/REPORT.md and ablations.json ARE tracked: they
# are the empirical evidence ADR-0015 cites.
